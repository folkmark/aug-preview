{
  "$schema": "https://playground.wordpress.net/blueprint-schema.json",
  "preferredVersions": {
    "php": "8.0",
    "wp": "6.5"
  },
  "steps": [
    {
      "step": "defineWpConfigConsts",
      "consts": {
        "WP_DEBUG": true,
        "WP_DEBUG_LOG": "/wordpress/wp-content/debug.log",
        "WP_DEBUG_DISPLAY": false
      }
    },
    {
      "step": "setSiteOptions",
      "options": {
        "permalink_structure": "/%postname%/"
      }
    },
    {
      "step": "activateTheme",
      "themeFolderName": "aerdf-stub"
    },
    {
      "step": "activatePlugin",
      "pluginPath": "augmented-ed/augmented-ed.php"
    },
    {
      "step": "runPHP",
      "code": "<?php require '/wordpress/wp-load.php'; flush_rewrite_rules(); wp_insert_term('Leadership','category',array('slug'=>'leadership')); $id = wp_insert_post(array('post_type'=>'team','post_status'=>'publish','post_title'=>'Sherry Lachman','post_name'=>'sherry-lachman','post_content'=>'<p>AERDF bio.</p>')); wp_set_post_terms($id, array(get_term_by('slug','leadership','category')->term_id), 'category'); $id = wp_insert_post(array('post_type'=>'team','post_status'=>'publish','post_title'=>'Caitlin Mills','post_name'=>'caitlin-mills','post_content'=>'<p>AERDF bio.</p>')); augmented_ed_create_pages(); foreach (get_posts(array('post_type'=>'page','post_status'=>'draft','numberposts'=>-1)) as $p) { wp_update_post(array('ID'=>$p->ID,'post_status'=>'publish')); } $dry = augmented_ed_import_team(array('dry_run'=>true)); $real = augmented_ed_import_team(); $again = augmented_ed_import_team(); $team = augmented_ed_data( 'team' ); $was = $team['archived'][0]; $wid = wp_insert_post(array('post_type'=>'team','post_status'=>'publish','post_title'=>$was['name'],'post_name'=>$was['slug'],'post_content'=>'<p>A bio from before they left the page.</p>')); wp_set_post_terms($wid, array(get_term_by('slug','augmented-team','category')->term_id, get_term_by('slug',$team['groups'][0]['slug'],'category')->term_id), 'category'); update_post_meta($wid, 'augmented_ed_sort', '3'); update_post_meta($wid, '_augmented_ed_import_hash', augmented_ed_meta_hash($wid)); $arch = augmented_ed_import_team(); $arch2 = augmented_ed_import_team(); file_put_contents('/wordpress/matrix.json', json_encode(array('php'=>PHP_VERSION,'wp'=>get_bloginfo('version'),'dry'=>$dry['counts'],'real'=>$real['counts'],'again'=>$again['counts'],'arch'=>$arch['counts'],'arch2'=>$arch2['counts'],'archived_slug'=>$was['slug'],'errors'=>array_merge($dry['errors'],$real['errors'])))); ?>"
    }
  ]
}
